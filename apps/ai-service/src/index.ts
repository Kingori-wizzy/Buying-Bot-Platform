/**
 * AI service application shell.
 * Feature implementation is intentionally deferred.
 *
 * Shared AI primitives will live in @buying-bot/ai-core; this app owns the service process.
 */
export function bootstrap(): void {
  // AI service bootstrap placeholder
}

bootstrap();
