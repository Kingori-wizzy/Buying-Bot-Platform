## Staging nginx

`staging.conf` routes Compose staging traffic (ADR-0019). TLS server block is
commented — EXTERNAL certificate paths required for HTTPS.

See also `docs/Deployment/PRODUCTION_ARCHITECTURE.md`.
